#include <iostream>
#include "src/window.h"
using namespace std;


int main() {
    string name = "FempitEngine TEST";
    const char* charname = name.c_str();
    cout << TEST << endl;
    OpenWindow(charname);
    return 0;
}